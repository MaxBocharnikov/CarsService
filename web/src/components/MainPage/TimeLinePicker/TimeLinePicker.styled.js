import styled from 'styled-components';
import Timeline from 'react-calendar-timeline'

const _TimeLinePicker = styled(Timeline)`
    .rct-header-root {
        background: #DED9CC;
    }
    
    .rct-dateHeader-primary {
        color: #993707;
    }
   
`;

const S = { _TimeLinePicker };

export default S;

