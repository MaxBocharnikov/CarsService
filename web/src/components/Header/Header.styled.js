import styled from 'styled-components';

import { Layout } from 'antd';
const { Header } = Layout;

const _Header = styled(Header)`
    background: #FFFBF0;
`;

const S = { _Header  };

export default S;