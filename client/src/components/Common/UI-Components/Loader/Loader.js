import React from 'react';
import S from './Loader.styled';

const Loader = () => {
    return (
        <S.LoaderWrapper>
            <S.LoaderElement/>
        </S.LoaderWrapper>
    )
};

export default Loader;