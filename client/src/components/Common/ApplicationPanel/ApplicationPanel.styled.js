import styled from 'styled-components';

const AdditionIconWrapper = styled.div`
    position: absolute;
    right: 0;
    top: calc(50% - 4px);

    cursor: pointer;
`;


const S = { AdditionIconWrapper  };

export default S;