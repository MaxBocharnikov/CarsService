import styled from 'styled-components';

const Wrapper = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: 300px;
  border: 1px solid #f0f0f0;
  border-radius: 2px;
  
  z-index: 100;
`;

const S = { Wrapper  };

export default S;